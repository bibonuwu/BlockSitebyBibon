<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
     

        <!-- Стиль кнопок с анимацией и закругленными углами -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,2,0,2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#2489D3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Анимация при наведении -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <!-- Анимация нажатия -->
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.6*"/>

        </Grid.ColumnDefinitions>
        
        <Grid>
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF0575E6" Offset="0"/>
                    <GradientStop Color="#FF021B79" Offset="1"/>
                    <GradientStop Color="#FF02298A" Offset="0.85"/>
                </LinearGradientBrush>
            </Grid.Background>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label Foreground="White" FontSize="20" FontWeight="Bold">Block Companets</Label>
                <Label FontSize="16" Foreground="White">Windows 10 regidet blocks</Label>

            </StackPanel>
        </Grid>
       
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1" Width="180">
            <Label FontWeight="Bold" FontSize="18">Hello Again!</Label>
            <Label>Welcome Back</Label>
            <Button Margin="0,10,0,0" Click="Button_Click_1">Диспечер задач ON</Button>
            <Button Margin="0,10,0,10" Click="Button_Click">Диспечер задач OFF</Button>
            <Button Click="Button_Click_3">Пуск ON</Button>
            <Button Margin="0,10,0,10" Click="Button_Click_2">Пуск OFF</Button>
            <Button Click="Button_Click_4">Block sites ON</Button>
            <Button Margin="0,10,0,10" Click="Button_Click_5">Block sites OFF</Button>
            <Button Click="Button_Click_6">Параметры OFF</Button>
            <Button Margin="0,10,0,0" Click="Button_Click_7">Параметры ON</Button>
        </StackPanel>

    </Grid>
</Window>
